@model List<ExperinceListDto>

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<div class="text-right mb-2">
    <a asp-action="Add" asp-controller="Experince" class="btn btn-outline-dark">Yeni Deneyim</a>
</div>
<table class="table table-hover table-striped table-bordered table-sm">
    <thead>
        <tr>
            <th>Ünvan</th>
            <th>Firma</th>
            <th>Açıklama</th>
            <th>Başlangıç Tarihi</th>
            <th>Bitiş Tarihi</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.SubTitle</td>
                <td>@item.Description</td>
                <td>@item.StartDate</td>
                <td>@item.EndDate</td>
                <td>
                    <a asp-action="Update" asp-controller="Experince" asp-route-id="@item.Id" class="btn btn-warning btn-sm mr-1">Güncelle</a>
                    <a asp-action="Delete" asp-controller="Experince" asp-route-id="@item.Id" onclick=" return confirm('Silmek istediğinize emin misiniz?')" class="btn btn-danger btn-sm">Sil</a>
                </td>
            </tr>
        }
    </tbody>

</table>